import { PageShell } from '@/components/ui';

export const metadata = { title: 'Clubs' };

export default function AdminClubsPage() {
  return (
    <PageShell title="Clubs" description="Manage clubs and organisations.">
      <p className="text-[var(--color-text-muted)]">Clubs management coming in a future phase.</p>
    </PageShell>
  );
}
